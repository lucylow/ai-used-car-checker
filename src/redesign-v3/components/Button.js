import React from 'react'; import {Pressable,View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; import Icon from './Icon'; export default function Button({title,onPress,icon,variant='primary'}){const p={primary:[COLORS.blue,COLORS.white,COLORS.blue],cyan:[COLORS.cyan,COLORS.ink,COLORS.cyan],outline:['transparent',COLORS.white,COLORS.line],ghost:['transparent',COLORS.cyan,'transparent']}[variant]||[COLORS.blue,COLORS.white,COLORS.blue];return <Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel={title}><View style={{height:50,borderRadius:RADIUS.pill,borderWidth:1,borderColor:p[2],backgroundColor:p[0],alignItems:'center',justifyContent:'center',flexDirection:'row',gap:8,paddingHorizontal:18}}>{icon?<Icon name={icon} size={18} color={p[1]}/>:null}<Text variant="bodyStrong" color={p[1]}>{title}</Text></View></Pressable>;}