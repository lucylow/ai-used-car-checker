import React from 'react'; import {Ionicons} from '@expo/vector-icons'; import {COLORS} from '../tokens'; export default function Icon({name,size=21,color=COLORS.white}){return <Ionicons name={name} size={size} color={color}/>;}