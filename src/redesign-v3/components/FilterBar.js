import React from 'react'; import {ScrollView} from 'react-native'; import FilterChip from './FilterChip'; export default function FilterBar({items,selected,onChange}){return <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{gap:8}}>{items.map(x=><FilterChip key={x} label={x} selected={x===selected} onPress={()=>onChange?.(x)}/>)}</ScrollView>;}
