import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; export default function MiniChart({values=[2,4,5,3,7,6],tone=COLORS.cyan}){const max=Math.max(...values,1);return <View style={{height:54,flexDirection:'row',gap:4,alignItems:'flex-end'}}>{values.map((v,i)=><View key={i} style={{flex:1,height:Math.max(5,v/max*54),backgroundColor:tone,borderRadius:RADIUS.sm,opacity:.35+i/values.length*.55}}/>)}</View>;}
