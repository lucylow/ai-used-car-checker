import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; export default function ScorePill({score=68}){const c=score>=75?COLORS.coral:score>=50?COLORS.amber:COLORS.mint;return <View style={{paddingHorizontal:11,paddingVertical:7,borderRadius:RADIUS.pill,backgroundColor:`${c}12`,borderWidth:1,borderColor:`${c}35`}}><Text variant="caption" color={c}>{score}/100</Text></View>;}
