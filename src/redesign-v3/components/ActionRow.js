import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; import Icon from './Icon'; export default function ActionRow({icon='chevron-forward',title,subtitle,onPress,tone=COLORS.cyan}){return <View onTouchEnd={onPress} style={{minHeight:62,paddingVertical:9,flexDirection:'row',alignItems:'center',gap:11,borderBottomWidth:1,borderBottomColor:COLORS.line}}><View style={{width:40,height:40,borderRadius:12,backgroundColor:COLORS.surface2,alignItems:'center',justifyContent:'center'}}><Icon name={icon} color={tone}/></View><View style={{flex:1}}><Text variant="bodyStrong">{title}</Text>{subtitle?<Text variant="caption" muted style={{marginTop:2}}>{subtitle}</Text>:null}</View><Icon name="chevron-forward" size={18} color={COLORS.muted}/></View>;}
