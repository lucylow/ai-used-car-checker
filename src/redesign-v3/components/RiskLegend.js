import React from 'react'; import {View} from 'react-native'; import {COLORS} from '../tokens'; import Text from './Text'; export default function RiskLegend(){return <View style={{flexDirection:'row',gap:12,flexWrap:'wrap'}}>{[['Low',COLORS.mint],['Watch',COLORS.amber],['Critical',COLORS.coral]].map(([x,c])=><View key={x} style={{flexDirection:'row',alignItems:'center',gap:6}}><View style={{width:8,height:8,borderRadius:99,backgroundColor:c}}/><Text variant="caption" muted>{x}</Text></View>)}</View>;}
