import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; import Icon from './Icon';
export default function OfflineBanner({show=true}){if(!show)return null;return <View style={{padding:11,borderRadius:RADIUS.md,backgroundColor:'rgba(244,183,64,.08)',borderWidth:1,borderColor:'rgba(244,183,64,.18)',flexDirection:'row',gap:8}}><Icon name="cloud-offline" size={16} color={COLORS.amber}/><Text variant="caption" color={COLORS.amber} style={{flex:1}}>Offline-safe mode · your inspection is saved on device.</Text></View>;}
