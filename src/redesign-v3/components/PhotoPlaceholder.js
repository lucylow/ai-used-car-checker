import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Icon from './Icon'; import Text from './Text'; export default function PhotoPlaceholder({label='Add photo',subtitle='Tap to capture evidence',onPress}){return <View onTouchEnd={onPress} style={{height:150,borderRadius:RADIUS.xl,borderWidth:1,borderStyle:'dashed',borderColor:COLORS.line,backgroundColor:COLORS.surface,alignItems:'center',justifyContent:'center'}}><Icon name="camera-outline" size={28} color={COLORS.cyan}/><Text variant="bodyStrong" style={{marginTop:8}}>{label}</Text><Text variant="caption" muted style={{marginTop:3}}>{subtitle}</Text></View>;}
