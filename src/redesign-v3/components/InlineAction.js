import React from 'react'; import {View} from 'react-native'; import {COLORS} from '../tokens'; import Icon from './Icon'; import Text from './Text'; export default function InlineAction({icon='arrow-forward',label,onPress}){return <View onTouchEnd={onPress} style={{alignSelf:'flex-start',flexDirection:'row',alignItems:'center',gap:6,minHeight:44}}><Text variant="bodyStrong" color={COLORS.cyan}>{label}</Text><Icon name={icon} size={16} color={COLORS.cyan}/></View>;}
