import React from 'react'; import {Image,View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; import Badge from './Badge'; export default function ComparableCard({item}){return <View style={{borderRadius:RADIUS.lg,overflow:'hidden',backgroundColor:COLORS.surface,borderWidth:1,borderColor:COLORS.line}}><Image source={{uri:item.image}} style={{height:132,width:'100%'}} resizeMode="cover"/><View style={{padding:12}}><Text variant="bodyStrong">{item.title}</Text><Text variant="caption" muted style={{marginTop:3}}>{item.mileage.toLocaleString()} mi · {item.city}</Text><View style={{marginTop:8,flexDirection:'row',justifyContent:'space-between',alignItems:'center'}}><Text variant="h3">${item.price.toLocaleString()}</Text><Badge label="Comparable" tone="info"/></View></View></View>;}
