import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; export default function SegmentedTabs({tabs,active,onChange}){return <View style={{flexDirection:'row',padding:4,borderRadius:RADIUS.md,backgroundColor:COLORS.surface,gap:4}}>{tabs.map(x=><View key={x} onTouchEnd={()=>onChange?.(x)} style={{flex:1,minHeight:38,borderRadius:10,alignItems:'center',justifyContent:'center',backgroundColor:active===x?COLORS.blue:'transparent'}}><Text variant="caption" color={active===x?COLORS.white:COLORS.muted}>{x}</Text></View>)}</View>;}
