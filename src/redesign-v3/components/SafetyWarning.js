import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Badge from './Badge'; import Text from './Text'; export default function SafetyWarning({children='This is informational and should be confirmed by a qualified professional when safety is involved.'}){return <View style={{padding:13,borderRadius:RADIUS.md,borderWidth:1,borderColor:'rgba(241,107,107,.22)',backgroundColor:'rgba(241,107,107,.07)'}}><Badge label="SAFETY" tone="danger"/><Text variant="caption" muted style={{marginTop:6}}>{children}</Text></View>;}
