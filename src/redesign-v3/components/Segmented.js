import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text';
export default function Segmented({items,active,onChange}){return <View style={{flexDirection:'row',padding:4,borderRadius:RADIUS.md,backgroundColor:COLORS.surface,gap:4}}>{items.map(x=><View key={x} onTouchEnd={()=>onChange?.(x)} style={{flex:1,minHeight:40,borderRadius:11,alignItems:'center',justifyContent:'center',backgroundColor:active===x?COLORS.blue:'transparent'}}><Text variant="caption" color={active===x?COLORS.white:COLORS.muted}>{x}</Text></View>)}</View>;}
