import React,{useState} from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; import Icon from './Icon'; export default function AIReasoning({steps=['Photo evidence','Checklist note','Market context']}){const[o,setO]=useState(false);return <View style={{borderRadius:RADIUS.md,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface2}}><View onTouchEnd={()=>setO(v=>!v)} style={{padding:12,flexDirection:'row'}}><Text variant="bodyStrong" color={COLORS.cyan} style={{flex:1}}>How CarWise reasoned</Text><Icon name={o?'chevron-up':'chevron-down'} color={COLORS.muted}/></View>{o?<View style={{padding:12,paddingTop:0}}>{steps.map((x,i)=><View key={x} style={{flexDirection:'row',gap:8,paddingVertical:6}}><Text variant="caption" color={COLORS.cyan}>{i+1}</Text><Text variant="caption" muted>{x}</Text></View>)}</View>:null}</View>;}
