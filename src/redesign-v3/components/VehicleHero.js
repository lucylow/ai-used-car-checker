import React from 'react'; import {Image,View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; import Badge from './Badge'; import {DEMO_VEHICLE} from '../data/demoData'; import {money,mileage} from '../services/format'; export default function VehicleHero({vehicle=DEMO_VEHICLE}){return <View style={{overflow:'hidden',borderRadius:RADIUS.xl,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface}}><Image source={{uri:vehicle.hero}} style={{width:'100%',height:205}} resizeMode="cover"/><View style={{padding:16}}><View style={{flexDirection:'row',justifyContent:'space-between',gap:8}}><View style={{flex:1}}><Text variant="h2">{vehicle.year} {vehicle.make} {vehicle.model}</Text><Text variant="caption" muted style={{marginTop:4}}>{mileage(vehicle.mileage)} · {vehicle.location}</Text></View><Badge label={`${vehicle.risk}/100`} tone="watch"/></View><View style={{flexDirection:'row',gap:8,marginTop:13}}><View style={{flex:1}}><Text variant="caption" muted>Asking</Text><Text variant="h3" style={{marginTop:2}}>{money(vehicle.askingPrice)}</Text></View><View style={{flex:1}}><Text variant="caption" muted>Market</Text><Text variant="h3" color={COLORS.mint} style={{marginTop:2}}>{money(vehicle.marketValue)}</Text></View></View></View></View>;}