import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; export default function SignatureBox({label='Buyer signature'}){return <View style={{height:120,borderRadius:RADIUS.md,borderWidth:1,borderColor:COLORS.line,borderStyle:'dashed',backgroundColor:COLORS.surface,justifyContent:'flex-end',padding:12}}><View style={{borderBottomWidth:1,borderBottomColor:COLORS.muted,width:'70%',height:58}}/><Text variant="caption" muted style={{marginTop:7}}>{label}</Text></View>;}
