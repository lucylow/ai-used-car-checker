import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; export default function AIQuestion({children,onPress}){return <View onTouchEnd={onPress} style={{padding:12,borderRadius:RADIUS.pill,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface}}><Text variant="caption" color={COLORS.white}>{children}</Text></View>;}
