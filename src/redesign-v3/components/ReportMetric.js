import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; export default function ReportMetric({label,value,tone=COLORS.cyan}){return <View style={{padding:13,borderRadius:RADIUS.md,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface}}><Text variant="caption" muted>{label}</Text><Text variant="h2" color={tone} style={{marginTop:4}}>{value}</Text></View>;}
