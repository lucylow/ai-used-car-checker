import React from 'react'; import {View} from 'react-native'; import {COLORS} from '../tokens'; import Text from './Text'; export default function KpiRow({label,value,change}){return <View style={{minHeight:48,flexDirection:'row',alignItems:'center',justifyContent:'space-between',borderBottomWidth:1,borderBottomColor:COLORS.line}}><Text variant="caption" muted>{label}</Text><View style={{flexDirection:'row',gap:8,alignItems:'center'}}><Text variant="bodyStrong">{value}</Text>{change?<Text variant="caption" color={COLORS.mint}>{change}</Text>:null}</View></View>;}
