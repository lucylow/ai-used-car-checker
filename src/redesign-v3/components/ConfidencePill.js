import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; export default function ConfidencePill({value=.92}){const c=value>=.85?COLORS.mint:value>=.65?COLORS.amber:COLORS.coral;return <View style={{paddingHorizontal:9,paddingVertical:6,borderRadius:RADIUS.pill,backgroundColor:`${c}12`}}><Text variant="caption" color={c}>{Math.round(value*100)}% confidence</Text></View>;}
