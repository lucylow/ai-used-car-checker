import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Icon from './Icon'; import Text from './Text'; export default function CaptureHint({title='Good framing',detail='Hold steady and move closer'}){return <View style={{padding:10,borderRadius:RADIUS.pill,backgroundColor:'rgba(7,17,31,.78)',flexDirection:'row',gap:7,alignItems:'center'}}><Icon name="checkmark-circle" size={15} color={COLORS.mint}/><Text variant="caption">{title}</Text><Text variant="caption" muted>· {detail}</Text></View>;}
