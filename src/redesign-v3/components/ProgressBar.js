import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; export default function ProgressBar({value=0,color=COLORS.cyan}){return <View style={{height:7,borderRadius:RADIUS.pill,overflow:'hidden',backgroundColor:COLORS.surface2}}><View style={{width:`${Math.max(0,Math.min(1,value))*100}%`,height:'100%',borderRadius:RADIUS.pill,backgroundColor:color}}/></View>;}