import React from 'react'; import {View} from 'react-native'; import {COLORS,SPACE} from '../tokens'; import IconButton from './IconButton'; import Text from './Text'; export default function ScreenHeader({title,subtitle,onBack,right,eyebrow}){return <View style={{paddingTop:8,paddingBottom:12}}><View style={{flexDirection:'row',alignItems:'center',gap:10}}>{onBack?<IconButton icon="chevron-back" label="Go back" onPress={onBack}/>:null}<View style={{flex:1}}>{eyebrow?<Text variant="caption" color={COLORS.cyan}>{eyebrow.toUpperCase()}</Text>:null}<Text variant="h1">{title}</Text>{subtitle?<Text muted style={{marginTop:3}}>{subtitle}</Text>:null}</View>{right}</View></View>;}