import React from 'react'; import {View} from 'react-native'; import {COLORS} from '../tokens'; import Text from './Text'; import IconButton from './IconButton'; export default function AppHeader({title='CarWise',subtitle,onMenu,onBell}){return <View style={{paddingTop:8,paddingBottom:10,flexDirection:'row',alignItems:'center',gap:10}}><View style={{flex:1}}><Text variant="h2">{title}</Text>{subtitle?<Text variant="caption" muted style={{marginTop:2}}>{subtitle}</Text>:null}</View>{onBell?<IconButton icon="notifications-outline" label="Notifications" color={COLORS.cyan} onPress={onBell}/>:null}{onMenu?<IconButton icon="menu-outline" label="Menu" onPress={onMenu}/>:null}</View>;}
