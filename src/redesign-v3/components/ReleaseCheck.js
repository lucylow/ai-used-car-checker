import React from 'react'; import {View} from 'react-native'; import {COLORS} from '../tokens'; import Icon from './Icon'; import Text from './Text'; export default function ReleaseCheck({label,done=true,detail}){return <View style={{flexDirection:'row',gap:10,paddingVertical:10,borderBottomWidth:1,borderBottomColor:COLORS.line}}><Icon name={done?'checkmark-circle':'ellipse-outline'} color={done?COLORS.mint:COLORS.amber} size={19}/><View style={{flex:1}}><Text variant="bodyStrong">{label}</Text>{detail?<Text variant="caption" muted style={{marginTop:2}}>{detail}</Text>:null}</View></View>;}
