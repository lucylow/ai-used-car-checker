import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; export default function MetricTile({label,value,detail,tone=COLORS.white}){return <View style={{padding:12,borderRadius:RADIUS.lg,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface,flex:1}}><Text variant="caption" muted>{label}</Text><Text variant="h2" color={tone} style={{marginTop:4}}>{value}</Text>{detail?<Text variant="caption" muted style={{marginTop:3}}>{detail}</Text>:null}</View>;}
