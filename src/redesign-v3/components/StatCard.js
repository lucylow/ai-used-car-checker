import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Icon from './Icon'; import Text from './Text';
export default function StatCard({label,value,sub,icon='analytics',tone=COLORS.cyan}){return <View style={{flex:1,minWidth:100,padding:14,borderRadius:RADIUS.lg,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface}}><Icon name={icon} size={17} color={tone}/><Text variant="caption" muted style={{marginTop:10}}>{label}</Text><Text variant="h2" style={{marginTop:3}}>{value}</Text>{sub?<Text variant="caption" color={tone} style={{marginTop:3}}>{sub}</Text>:null}</View>;}
