import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Badge from './Badge'; import Text from './Text'; export default function NotificationCard({title,message,tone='info',onPress}){return <View onTouchEnd={onPress} style={{padding:14,borderRadius:RADIUS.lg,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface,marginBottom:9}}><View style={{flexDirection:'row',justifyContent:'space-between',gap:8}}><Text variant="bodyStrong" style={{flex:1}}>{title}</Text><Badge label="NEW" tone={tone}/></View><Text variant="caption" muted style={{marginTop:5}}>{message}</Text></View>;}
