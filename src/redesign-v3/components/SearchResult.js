import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Icon from './Icon'; import Text from './Text'; export default function SearchResult({title,detail,icon='search-outline',onPress}){return <View onTouchEnd={onPress} style={{padding:13,borderRadius:RADIUS.md,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface,flexDirection:'row',gap:10}}><Icon name={icon} color={COLORS.cyan}/><View style={{flex:1}}><Text variant="bodyStrong">{title}</Text><Text variant="caption" muted style={{marginTop:2}}>{detail}</Text></View><Icon name="chevron-forward" size={17} color={COLORS.muted}/></View>;}
