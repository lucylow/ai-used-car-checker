import React,{useEffect,useRef} from 'react'; import {Animated} from 'react-native'; export default function MotionFade({children,delay=0}){const a=useRef(new Animated.Value(0)).current;useEffect(()=>{const id=setTimeout(()=>Animated.timing(a,{toValue:1,duration:240,useNativeDriver:true}).start(),delay);return()=>clearTimeout(id)},[delay,a]);return <Animated.View style={{opacity:a}}>{children}</Animated.View>;}
