import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; export default function SettingGroup({title,children}){return <View style={{marginTop:20}}><Text variant="caption" color={COLORS.cyan} style={{letterSpacing:1}}>{title.toUpperCase()}</Text><View style={{marginTop:8,padding:4,borderRadius:RADIUS.lg,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface}}>{children}</View></View>;}
