import React from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Icon from './Icon'; import Text from './Text'; import Badge from './Badge'; export default function ChecklistRow({item,onPress}){const map={pass:['checkmark',COLORS.mint,'Pass','success'],watch:['remove',COLORS.amber,'Watch','watch'],issue:['close',COLORS.coral,'Issue','danger']}[item.status]||['remove',COLORS.amber,'Watch','watch'];return <View onTouchEnd={onPress} style={{padding:13,borderRadius:RADIUS.md,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface,flexDirection:'row',gap:10,alignItems:'center'}}><View style={{width:38,height:38,borderRadius:11,backgroundColor:COLORS.surface2,alignItems:'center',justifyContent:'center'}}><Icon name={map[0]} color={map[1]}/></View><View style={{flex:1}}><Text variant="bodyStrong">{item.title}</Text><Text variant="caption" muted numberOfLines={1}>{item.note}</Text></View><Badge label={map[2]} tone={map[3]}/></View>;}