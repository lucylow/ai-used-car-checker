import React,{useState} from 'react'; import {View} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Text from './Text'; import Button from './Button'; import VoiceWave from './VoiceWave'; export default function VoiceRecorder(){const[r,setR]=useState(false);return <View style={{padding:14,borderRadius:RADIUS.lg,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface}}><Text variant="h3">Voice note</Text><Text variant="caption" muted style={{marginTop:3}}>Capture an observation without typing.</Text><View style={{marginTop:10}}><VoiceWave/></View><Button title={r?'Stop recording':'Record note'} icon={r?'stop':'mic'} variant={r?'ghost':'outline'} onPress={()=>setR(v=>!v)}/></View>;}
