import React from 'react'; import {Pressable} from 'react-native'; import {COLORS,RADIUS} from '../tokens'; import Icon from './Icon'; export default function IconButton({icon='chevron-back',label='Action',onPress,color=COLORS.white}){return <Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel={label} style={{width:46,height:46,borderRadius:RADIUS.pill,borderWidth:1,borderColor:COLORS.line,backgroundColor:COLORS.surface,alignItems:'center',justifyContent:'center'}}><Icon name={icon} color={color}/></Pressable>;}